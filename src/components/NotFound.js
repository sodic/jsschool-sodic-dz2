import React, { Component } from 'react';

class NotFound extends Component {
    render() {
        return (
            <h1>We still don't have this pallete :(</h1>
        );
    }
}

export default NotFound;